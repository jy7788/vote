<view class="home">
<van-tabs active="{{ active }}" bind:change="onChange">
  <van-tab title="服用胶囊排名">
  <van-nav-bar
  title="服用胶囊排名"
  />
<van-panel use-footer-slot>
  <view class="align">
  商场经理:<van-progress percentage="{{ queen_manager }}" />
  </view>
  <view class="align">
  导购:<van-progress percentage="{{ queen_guide }}" />
  </view>
  <view class="align">
  医生:<van-progress percentage="{{ queen_doctor }}" />
  </view>
  <view class="align">
  保洁员:<van-progress percentage="{{ queen_cleaner }}" />
  </view>
</van-panel>
  </van-tab>
  <van-tab title="投票排名">
  <view class="align" wx:for="{{ rankArr }}" >
    第{{index + 1}}
    <van-icon size="40px"
    name="{{item.wxUserInfo.avatarUrl}}" info="{{item.wxUserInfo.nickName}}"/>
    答对{{item.successCount}}题
  </view>
  </van-tab>
</van-tabs>


</view>